{
  "name": "safebet-bot-safe-report",
  "version": "2.1.0",
  "type": "module",
  "scripts": {
    "build": "mkdir -p dist && cp index.html dist/index.html && cp manifest.json dist/manifest.json && cp sw.js dist/sw.js && cp icon.svg dist/icon.svg"
  },
  "dependencies": {}
}