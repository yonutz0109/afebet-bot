{
  "name": "afebet-bot",
  "version": "7.3.8",
  "scripts": {
    "build": "mkdir -p public_out && cp index.html public_out/index.html && cp icon.svg public_out/icon.svg 2>/dev/null || true && cp -R public/* public_out/ 2>/dev/null || true"
  },
  "dependencies": {},
  "type": "module"
}
