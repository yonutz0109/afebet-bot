{
  "version": 2,
  "buildCommand": "mkdir -p public_out && cp index.html public_out/index.html && cp icon.svg public_out/icon.svg 2>/dev/null || true && cp -R public/* public_out/ 2>/dev/null || true",
  "outputDirectory": "public_out",
  "functions": {
    "api/*.js": {
      "maxDuration": 30,
      "includeFiles": "api/**"
    }
  },
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/$1"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
